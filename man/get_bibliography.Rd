% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_bibliography.R
\name{get_bibliography}
\alias{get_bibliography}
\title{Get bibliography from an object}
\usage{
get_bibliography(x, format = c("R", "bibtex"))
}
\arguments{
\item{x}{An object (e.g., vpts, pvol) with \code{attributes$references}.}

\item{format}{Character string indicating the output format. Either "R" (default)
for R bibentry objects, or "bibtex" for BibTeX format.}
}
\value{
If \code{format = "R"},   list of bibentry objects from the object's
\code{attributes$references}. If \code{format = "bibtex"},   character vector of
BibTeX entries. Returns \code{NULL} if no references exist or if the object
doesn't have the expected structure.
}
\description{
Retrieve the bibliography/references stored in an object's attributes.
Works with vpts, pvol, and other objects that have an \code{attributes$references} field.
}
\examples{
# Get bibliography as R objects (default) from   vpts object
bibliography <- get_bibliography(example_vpts)

# Get bibliography as BibTeX from   vpts object
bibtex_bibliography <- get_bibliography(example_vpts, format = "bibtex")
}
